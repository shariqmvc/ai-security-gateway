import {NavLink} from "react-router-dom";import {Bot,CircleDollarSign,KeyRound,LayoutDashboard,LogOut,Settings2,ShieldCheck,SlidersHorizontal} from "lucide-react";import {useAuth} from "../state/AuthContext";
const nav=[["Overview","/",LayoutDashboard],["Chat","/chat",Bot],["Models","/models",SlidersHorizontal],["API Keys","/api-keys",KeyRound],["Providers","/providers",Settings2],["Usage","/usage",CircleDollarSign],["Security","/security",ShieldCheck]] as const;
export function AppShell({children}:{children:React.ReactNode}){const{user,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">AR</div><div><div className="brand-name">AIRouter</div><div className="brand-sub">Personal v1</div></div></div><nav className="nav-list">{nav.map(([label,to,Icon])=><NavLink key={to} to={to} className={({isActive})=>`nav-item ${isActive?"active":""}`}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="user-card"><div className="avatar">{(user?.displayName||user?.email||"U").slice(0,1).toUpperCase()}</div><div className="user-meta"><strong>{user?.displayName||"Personal account"}</strong><span>{user?.email}</span></div></div><button className="ghost-button" onClick={()=>void logout()}><LogOut size={16}/> Sign out</button></div></aside><main className="main-content"><header className="topbar"><div><span className="eyebrow">PERSONAL AI GATEWAY</span><h1>Control plane</h1></div><div className="status-pill"><span className="status-dot"/>Backend connected</div></header><div className="content">{children}</div></main></div>}