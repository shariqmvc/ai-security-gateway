# Family-aware splitter reference for future master corpora.
# It deliberately does not perform random row-level splitting.
# Current package ships pre-split train/validation/test files.
print("Use build_corpus.py to validate the checked-in split.")
